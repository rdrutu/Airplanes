services:
  - type: web
    name: avioane-server
    env: node
    buildCommand: npm install && npm run build --workspace=packages/shared && npm run build --workspace=apps/server
    startCommand: node apps/server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
